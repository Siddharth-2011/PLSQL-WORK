1.
--trigger when updation of values in table
CREATE OR REPLACE TRIGGER T1
  AFTER UPDATE OF EMPID ON EMPLOYE3 FOR EACH ROW
   BEGIN
    DBMS_OUTPUT.PUT_LINE('OLD VALUE IS '||:OLD.EMPID);
    DBMS_OUTPUT.PUT_LINE('NEW VALUE IS '||:NEW.EMPID);
END;
DROP TRIGGER T1

2.
--trigger when updation of values in table
CREATE OR REPLACE TRIGGER T1
  AFTER UPDATE OF EMPID ON EMPLOYE3 
   BEGIN
    DBMS_OUTPUT.PUT_LINE('OLD VALUE IS '||:OLD.EMPID);
    DBMS_OUTPUT.PUT_LINE('NEW VALUE IS '||:NEW.EMPID);
END;

3.
--trigger after insertion or deletion on table
CREATE OR REPLACE TRIGGER T12
AFTER INSERT OR DELETE OR UPDATE ON EMPLOYEE
    BEGIN
         IF INSERTING THEN
         DBMS_OUTPUT.PUT_LINE('VALUE INSERTED');
         ELSIF UPDATING THEN 
                 DBMS_OUTPUT.PUT_LINE('VALUE UPDATED');
         ELSIF DELETING THEN
                 DBMS_OUTPUT.PUT_LINE('VALUE DELETED');
         END IF;
END;

SELECT * FROM EMPLOYEE
INSERT INTO EMPLOYEE VALUES("SHAINI",21, 5647890123, 50000,"56TY",'C')

